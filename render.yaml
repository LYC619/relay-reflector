services:
  - type: web
    name: api-log
    runtime: docker
    plan: free
    envVars:
      - key: UPSTREAM_URL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: PORT
        value: 7891
    disk:
      name: api-log-data
      mountPath: /data
      sizeGB: 1
